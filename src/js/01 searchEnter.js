'use strict';

// La tecla enter
function handlesubmit(ev) {
  ev.preventDefault();
}

//miniButtonReset.addEventListener('click', handleMiniClickReset);
form.addEventListener('submit', handlesubmit);
